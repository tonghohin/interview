# AI Provider API Keys
# You only need ONE of these - choose your preferred AI provider

# OpenAI (GPT-3.5, GPT-4)
OPENAI_API_KEY=your_openai_api_key_here

# Anthropic (Claude)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Optional: Specify which provider to use (openai, anthropic, etc.)
AI_PROVIDER=openai

# Optional: Server port (default: 3000)
PORT=3000
