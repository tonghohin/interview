{
    "name": "interview",
    "private": true,
    "scripts": {
        "dev": "pnpm -r --parallel dev",
        "build": "pnpm -r build",
        "start": "pnpm -r start"
    },
    "pnpm": {
        "overrides": {
            "@types/react": "19.2.13",
            "@types/react-dom": "19.2.3"
        }
    }
}
